# marketplace-payouts

Deploy repo for the **payouts** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs payouts --push`.

Coolify: base_directory `/`, dockerfile `/apps/payouts/Dockerfile`.
